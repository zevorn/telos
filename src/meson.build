subdir('base')
subdir('state')
subdir('store')
subdir('actor')

telos_dep = declare_dependency(
    dependencies: [
        telos_base_dep,
        telos_state_dep,
        telos_store_dep,
        telos_actor_dep,
    ],
)
